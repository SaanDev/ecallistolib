name: Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']
        dependency-set: [min-supported, latest]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ "${{ matrix.dependency-set }}" = "min-supported" ]; then
            if [ "${{ matrix.python-version }}" = "3.10" ]; then
              ASTROPY_SPEC="astropy==6.1.*"
            else
              ASTROPY_SPEC="astropy==7.2.*"
            fi
            pip install "numpy==2.0.*" "${ASTROPY_SPEC}"
          else
            pip install --upgrade numpy astropy matplotlib requests beautifulsoup4
          fi
          pip install -e ".[download,plot]"
          pip install pytest pytest-cov

      - name: Run tests
        run: |
          pytest -q

      - name: Generate coverage report
        if: matrix.python-version == '3.11' && matrix.dependency-set == 'latest'
        run: |
          pytest --cov=ecallistolib --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        if: matrix.python-version == '3.11' && matrix.dependency-set == 'latest'
        with:
          files: ./coverage.xml
          fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          # Keep CI lint rules reproducible across Ruff releases.
          pip install "ruff==0.15.2" mypy

      - name: Run ruff
        run: ruff check src/ tests/

      - name: Run mypy
        run: mypy src/ecallistolib --ignore-missing-imports --disable-error-code=import-untyped

  goes-extra:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install GOES and plotting extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[goes,plot]"
          pip install pytest

      - name: Run offline GOES tests
        run: pytest -q tests/test_goes.py
